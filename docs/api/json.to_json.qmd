# json.to_json { #duckops.json.to_json }

`json.to_json(*args)`

Create JSON from a value of any type. Our LIST is converted to a JSON array, and our STRUCT and MAP are converted to a JSON object

| duckdb example | result |
| -------------- | ------ |
| nan | nan |
