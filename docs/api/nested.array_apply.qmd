# nested.array_apply { #duckops.nested.array_apply }

`nested.array_apply(col0, col1)`

Alias for list_transform.

| duckdb example | result |
| -------------- | ------ |
| array_apply(l, x -> x + 1) | [5, 6, 7] |
