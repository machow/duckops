# nested.list_apply { #duckops.nested.list_apply }

`nested.list_apply(col0, col1)`

Alias for list_transform.

| duckdb example | result |
| -------------- | ------ |
| list_apply(l, x -> x + 1) | [5, 6, 7] |
