# nested.list_contains { #duckops.nested.list_contains }

`nested.list_contains(col0, col1)`

Returns true if the list contains the element.

| duckdb example | result |
| -------------- | ------ |
| list_contains([1, 2, NULL], 1) | true |
